/**
 * @file src/array/push.js
 */
'use strict'

/**
 * Pushes an element {el} onto the end of an Array {arr}.
 * @param  {Array} arr
 * @param  {*}     el
 * @return {Array}
 */
module.exports = (arr, el) => [...arr, el]
